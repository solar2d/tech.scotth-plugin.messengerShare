Sources for the plugin `plugin.messengerShare`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.messengerShare" = {
            publisherId = "tech.scotth",
        },
    },
}
```
